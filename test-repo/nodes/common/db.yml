mysql:
  cluster_map:
    common:
      endpoint: ${#haproxy.cluster_map.common.host}:3306
      hosts: ${#node_map.mysql.hosts}
      port: 13306
      user_map:
        slave:
          user: slave
          password: slavepass
          src_hosts:
            - '192.168.122.0/255.255.255.0'
          previleges: 'REPLICATION SLAVE'
        openstack:
          user: openstack
          password: openstackpass
          src_hosts:
            - '192.168.122.0/255.255.255.0'
          previleges: 'ALL PREVILEGES'
          dbs: [keystone, nova, novaapi, neutron, glance, cinder, heat, ceilometer, metering, alarm, event, aodh, manila, ironic, trove, designate, barbican]

      replication:
        types: [master, master]
